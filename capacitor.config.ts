import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.musetrainer.pitchdetector',
  appName: 'pitch-detector',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
